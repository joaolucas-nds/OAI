# Log — 2026-09-03

## Objetivo da conversa
Aplicar o template-update do KCM v1.122.0 (sair do layout flat antigo para `meta/` + `.claude/`, modo Code), revisar o pacote resubido e preparar a transferência para uma conversa nova.

## Feito
- **Varredura do mount em duas passagens** (antes e depois de o dono aplicar a migração em disco).
- **Carimbo de modos:** detectada sobra do modo ASU (marcador `## Saída de código via ASU (patch)` presente no CEREBRO antigo com ASU declarado `não`). Eliminada pela substituição do arquivo.
- **Linhas revogadas, varridas pelo FATO** (não só por string): REV-2 (bloco git pronto para copiar) e REV-4 (nunca blocos soltos) viviam no CEREBRO/CLAUDE antigos e morreram com o descarte; REV-1 (esperar sinal de upload) ausente; REV-3 (par sessão/turno) morreu nos antigos mas **sobreviveu num arquivo que fica** — cabeçalho `Última Sessão` no `meta/STATUS.md` (registrado na WO 0001).
- **Varredura de preservação antes do descarte:** o `CLAUDE.md`/CEREBRO antigo era 100% regra genérica do KCM, zero conteúdo personalizado; as convenções que continha já viviam no `CONTEXT.md` (específicas) e no CEREBRO novo (genéricas, refinadas). Descarte confirmado sem perda.
- **Comparação a/b/c dos 20 arquivos do pacote** contra os vivos. A maioria dos "novos" era placeholder de template (`## DEC-[N]`, `## F1 — [Nome]`) que os arquivos vivos já realizam com conteúdo real. Achados reais: título interno do `HISTORY.md`, cabeçalho `Última Sessão` do STATUS, e seções `Adiadas` + `Feedback para o Kit` no IDEAS.
- **Entregues:** `INSTRUCOES-DO-PROJETO.md` (instruções de projeto para a caixa do Claude), WO 0001 (deltas de STATUS e CONTEXT), este log e o handoff brief.

## Decisões
- **DEC-007** (migração para o contrato KCM v1.122.0) — registrada no `meta/DECISIONS.md` **[medido: presente]**.
- **ASU desligado** por decisão do dono: projeto em modo Code não precisa de ASU **[relatado pelo dono]**.
- **CLAUDE.md antigo descartado inteiro**, sem migrar nada — decisão do dono, confirmada por varredura.
- **Canal dos meta neste ciclo = CODE** para os deltas de STATUS/CONTEXT (WO 0001); arquivos novos vieram inteiros pelo chat.

## Bugs
Nenhum bug de código nesta conversa — trabalho inteiramente de estrutura e registro. O código não foi tocado: `main.py`, `matching_engine.py`, `test_matching.py` e `golden_set.csv` seguem como estavam.

## Aprendizados / armadilhas
- **O mount do Projeto é ACHATADO e existe `_MANIFEST_OAI.md` para desfazer isso.** Deduzi a estrutura do repo a partir dos nomes planos e concluí errado: escrevi uma instrução de limpeza para arquivos que o dono já havia colocado nos lugares certos. O manifesto traz o mapa nome-plano → caminho real, a raiz em disco e uma foto do Git. **Lê-lo é o primeiro passo de qualquer trabalho com caminhos.** → vira armadilha 10 no CONTEXT (WO 0001).
- **"Instrução" neste projeto significa instruções de projeto** (o texto da caixa do Claude, equivalente ao `INSTRUCOES__template-update.md`), não script de terminal. Pedido mal interpretado uma vez; corrigido.
- **Linha revogada morre por substituição quando o arquivo inteiro é trocado** — não precisa edição item a item. A varredura serve para achar o que sobreviveu em arquivos que FICAM (foi o caso do STATUS).
- **Comparar doc vivo contra template vazio gera falso-positivo em massa:** quase todo "cabeçalho novo" é molde. Filtrar pelo que é estrutura real vs. placeholder.
- **Relato ≠ instrução na revogação:** "ao final de cada sessão, entregue X" é instrução e sai; "isso custou uma sessão inteira" é fato histórico e fica. Reescrever relato falsifica o registro.

## Onde parei
Migração KCM v1.122.0 aplicada em disco pelo dono e verificada por instrumento: `meta/` e `.claude/` existem, `CEREBRO.md` em v1.122.0 sem seção ASU, DEC-007 + CHANGELOG 0.4.1 + `Feedback para o Kit` presentes, título do `HISTORY.md` corrigido. Resta aplicar a WO 0001 (dois deltas) e commitar.

**Estado do Git [medido por instrumento — `_MANIFEST_OAI.md`, foto de 2026-09-03 22:30]:** último commit `72f38c5 2026-06-27 first commit`; branch `main` sincronizado com `origin/main`; **10 modificados, 8 não rastreados**; 5 arquivos do mount fora do commit (`meta/CEREBRO.md`, `meta/CHANGELOG.md`, `meta/CONTEXT.md`, `meta/DECISIONS.md`, `meta/IDEAS.md`). **A migração ainda não estava commitada nessa foto.**

## Próximos passos
1. Aplicar a WO 0001 (`/apply-wo meta/workorders/260903-wo0001-registro-fecho-migracao.md`).
2. Colar o conteúdo de `INSTRUCOES-DO-PROJETO.md` na caixa de instruções do Projeto e salvar o arquivo na raiz do repo.
3. `git add -A && git commit && git push` — fechar a migração no repositório.
4. Retomar código: **integrar `UTILITÁRIOS/matching_engine.py` na GUI** (`main.py` ainda usa o `MotorMatching` antigo embutido) — item 1 do backlog do STATUS.

---

## Conversa 2 (Code) — /apply-wo wo0001 + /wrap

### Feito
- **WO 0001 aplicada.** Cabeçalho «Âncoras lidas em» preenchido → aplicável. As duas âncoras casaram EXATAMENTE; nenhuma guarda de idempotência disparou.
  - `meta/STATUS.md`: `## 💬 Última Sessão` → `## 💬 Última conversa`; parágrafo de 2026-09-03 inserido ANTES do de 2026-05-31.
  - `meta/CONTEXT.md`: armadilha 10 (não deduzir estrutura do repo a partir do mount achatado → ler `_MANIFEST_OAI.md`) inserida entre a 9 e `## Contexto de Produto`.
- **Verificações 1-4 da WO:** todas OK (`## 💬 Última conversa` → 1; `Última Sessão` → 0; `O mount do Projeto é ACHATADO` → 1; `git status` mostra só os dois arquivos desta WO).
- **Commit `0658523`** (main, `72f38c5..0658523`), 2 files changed, 10 insertions(+), 1 deletion(-). Push feito.
- **/wrap:** append de fecho no STATUS; esta seção no log; commit de fecho com `meta/STATUS.md` + `logs/2026-09-03.md`.

### Decisões
Nenhuma DEC/FIX nova — trabalho de registro puro. DEC-007 (migração) já estava registrada.

### Bugs
Nenhum bug de código tocado nesta sessão. **Achado orfão:** a nota `../260814-1021.txt` (2026-08-14) relata dois bugs de código nunca registrados — (a) nome que começa com `0` perde o zero ao renomear; (b) match com código `00` (dois zeros seguidos) é ignorado. Anotado no STATUS («Pendência de código não registrada») para triagem; não investigado aqui.

### Aprendizados / armadilhas
- O `git diff` de `meta/CONTEXT.md` mostra também o chunk pré-existente «## Estrutura de Documentação (KCM v1.122.0)» — não é da WO, já estava na cópia de trabalho. Confirmado que as edições da WO são só as duas nomeadas.
- A medição datada «10 modificados e 8 não rastreados [22:30, `_MANIFEST_OAI.md`]» no STATUS é registro histórico com origem — NÃO reescrita no fecho; o estado atual foi acrescentado à parte.

### Onde parei
WO 0001 fechada e no repositório. A migração KCM restante (CEREBRO/CHANGELOG/DECISIONS/IDEAS modificados, 5 deleções, não rastreados incluindo `.claude/` e `INSTRUCOES-DO-PROJETO.md`) segue FORA do commit — fecho a cargo do dono (passo 2 e 3 dos «Próximos passos» acima).

### Próximos passos
1. Dono: colar `INSTRUCOES-DO-PROJETO.md` na caixa do Projeto + `git add -A && git commit && git push` para fechar a migração.
2. Triar os dois bugs da nota `260814-1021.txt` para IDEAS/STATUS antes de retomar o backlog.
3. Retomar código: integrar `matching_engine.py` na GUI — item 1 do backlog.
