# Log — 2026-09-04

## Objetivo da sessão
Retomar pelo handoff brief de 2026-09-03, fechar as pendências de registro e triar os dois bugs órfãos da nota `260814-1021.txt`, antes de abrir a frente de integração do motor v2 na GUI.

## Feito
- **Arranque completo** — `_MANIFEST_OAI.md` (foto de 2026-09-04 01:42) → `CEREBRO.md` (lido por inteiro, 423 linhas) → `CONTEXT.md` → `STATUS.md` → topo do `CHANGELOG.md` → `_TREE_OAI.md` → os três `.txt` avulsos do mount.
- **Conferência do handoff brief contra o estado real.** Duas das três pendências já estavam resolvidas e o brief não sabia: a WO 0001 foi aplicada (`0658523`) e fechada pelo `/wrap` (`a1ccb74`), e as instruções de projeto já estão instaladas na caixa (texto idêntico ao `INSTRUCOES-DO-PROJETO.md`, conferido). Só a terceira — commitar a migração KCM — continuava aberta.
- **Causa raiz dos dois bugs de 2026-08-14 encontrada e reproduzida** (ver Bugs).
- **WO 0002 escrita** (`meta/workorders/260904-wo0002-triagem-bugs-zero-a-esquerda.md`): fecha a migração KCM no passo 0 e registra a triagem em DECISIONS, STATUS e IDEAS.
- **WO 0002 aplicada e fechada** (`/apply-wo` + `/wrap`, mesma sessão). Dois commits, dois pushes: `b2d1c73` (chore, fecho da migração KCM — 21 arquivos) e `50b369e` (docs, registro do FIX-005 — DECISIONS/IDEAS/STATUS). Verificações da WO (âncoras, `grep` de cada inserção, `git diff --stat` restrito aos 3 arquivos) todas OK.
- **Higiene do STATUS no fecho:** duas pendências registradas em 2026-09-03 ficaram stale com a aplicação da WO — "commitar a migração" (resolvida pelo commit `b2d1c73`) e "bugs não registrados em IDEAS/DECISIONS" (resolvida por FIX-005; linha removida do STATUS por virar duplicata do item já em «Quebrado»). Ambas marcadas/corrigidas no `meta/STATUS.md`.
- **Nenhuma linha de código tocada.** O backlog de código segue intacto.

## Decisões
- **Não corrigir o defeito nesta conversa, de propósito.** A correção cai em `main.py` na mesma região que a integração do motor v2 vai refatorar. Fazer as duas na mesma frente evita dois diffs conflitantes no mesmo arquivo e um `main.py` meio-migrado entre commits. Registrado no próprio FIX-005.
- **A armadilha nova NÃO entra no CONTEXT ainda.** Entra junto com a correção — armadilha que descreve um defeito ainda vivo confunde quem lê a lista para saber do que já está protegido.
- **Dois commits, não um** — `chore` (migração) e `docs` (registro) são naturezas diferentes; um commit só faria o FIX-005 sumir dentro de uma migração de 17 arquivos.

## Bugs
- **FIX-005 — zero à esquerda comido na carga da planilha.** Os dois sintomas relatados pelo dono em 2026-08-14 têm **uma causa raiz só**: a planilha é lida sem `dtype=str` (`main.py:1300` `read_csv`, `main.py:1305` `read_excel`, `test_matching.py:31` `read_csv`), o pandas infere `int64` numa coluna numérica e `00611` vira `611` antes de qualquer `str()` do código.
  - Sintoma (a), nome: coluna do novo nome numérica → o arquivo é renomeado para `611`.
  - Sintoma (b), match ignorado: coluna de matching numérica → `611` tem 3 caracteres e **não casa** com `REGEX_CODIGO` (exige 4+), então a linha fica com zero códigos; o arquivo tem `00611`; não há match por código; a guarda DEC-006 devolve `código_ausente` e o produto é ignorado.
  - **Reproduzido** de forma determinística com pandas 3.0.2 em 2026-09-04, aplicando a regra bidirecional dos dois motores sobre o valor degradado.
  - **Por que o golden set não pega:** o harness casa contra a coluna descritiva `"Atual: 14/04/2026 - Anterior: 14/04/2026"`, que o pandas lê como string — o zero sobrevive ali. O defeito só aparece com coluna puramente numérica.
  - **Não provado:** qual coluna o dono tinha selecionado ao ver cada sintoma. A causa raiz está medida; o mapeamento sintoma→coluna é dedução.

## Aprendizados / armadilhas
- **Planilha é TEXTO.** A inferência de tipo do pandas é armadilha silenciosa em qualquer coluna que seja identificador: não dá erro, não avisa, e o dano só aparece no nome do arquivo final. Vira armadilha em `CONTEXT.md` junto com a correção.
- **Um golden set verde pode estar verde pelo motivo errado.** 24/24 convivia com um defeito real porque o harness exercita uma configuração de coluna (descritiva) diferente da que o dono usa na GUI (código pura). Cobertura de casos não é cobertura de **configurações**.
- **Documento derivado não é fonte.** O handoff brief de 2026-09-03 dizia que a WO 0001 e as instruções estavam pendentes; as duas já estavam feitas. Conferir no arquivo vivo custou três leituras e evitou refazer trabalho aplicado.
- **Duplicação de carga vira duplicação de bug.** O mesmo defeito existe em dois lugares porque a leitura da planilha foi escrita duas vezes. Corrigir «minimamente» nos dois seria garantir que voltem a divergir.

## Onde parei
WO 0002 **aplicada, fechada e empurrada**. Repositório em `50b369e`, working tree limpo, `main` sincronizada com `origin/main`. Migração KCM commitada (`b2d1c73`) e triagem do FIX-005 registrada em DECISIONS/STATUS/IDEAS (`50b369e`). STATUS.md sem pendências stale. Nenhuma linha de código tocada na sessão inteira — o backlog de código segue intacto, e o próximo passo óbvio é a frente de integração do motor v2 na GUI, já carregando a correção do FIX-005 dentro dela.

## Próximos passos
- Abrir a frente **integração do motor v2 na GUI**, incluindo: remover o `MotorMatching` embutido do `main.py` e importar de `UTILITÁRIOS/matching_engine.py`; criar o loader único de planilha com `dtype=str` + `keep_default_na=False` (corrige FIX-005 numa vez só, na GUI e no harness); exibir `componentes`, `codigo_casado` e a flag `ambiguo` na tabela.
- Rodar o golden set depois de mexer: `python UTILITÁRIOS/test_matching.py <planilha.csv> test/golden_set.csv 70` — alvo 24/24 e 80/80.
- Pedir ao dono a planilha real com uma coluna de código **pura**, para o golden set passar a exercitar o caso de zero à esquerda.
